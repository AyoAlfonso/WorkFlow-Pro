import React from "react";
import { render, screen } from "@testing-library/react";


//   describe("Scorecard search input", () => {
//     //We want to test that all it waits for all data to be returned, and re-renders on late return
//     //we want to make sure we are rendering all the types of dat user, company, and team
//     //we want to make sure all text is correct
//     //make sure that it is locked/open based on your payment plan 

//     test("input", () => {
//       render(<input type="text" defaultValue="" />);

//       expect(screen.getByDisplayValue("")).toBeInTheDocument();
//     });

//     test("textarea", () => {
//       render(<textarea defaultValue="" />);

//       expect(screen.getByDisplayValue("")).toBeInTheDocument();
//     });

//     test("select", () => {
//       render(
//         <select defaultValue="1">
//           <option value="1">Example 1</option>
//           <option value="2">Example 2</option>
//         </select>,
//       );

//       expect(screen.getByDisplayValue("Example 1")).toBeInTheDocument();
//     });
//   });
// });
