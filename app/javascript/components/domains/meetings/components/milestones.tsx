import * as React from "react";
import { Text } from "~/components/shared/text";

export const Milestones = (props: {}): JSX.Element => {
  return (
    <>
      <Text>
        Milestones Component. Fetch all personal milestones for this week and display them.
      </Text>
    </>
  );
};
