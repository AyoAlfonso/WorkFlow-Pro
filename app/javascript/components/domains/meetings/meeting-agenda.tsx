import * as React from "react";
import { Text } from "~/components/shared/text";

export interface IMeetingAgendaProps {
  //meeting
}

export const MeetingAgenda = (props: IMeetingAgendaProps): JSX.Element => {
  return <Text>Meeting Agenda Placeholder</Text>;
};
